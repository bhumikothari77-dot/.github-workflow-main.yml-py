name: CI Pipeline

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build step
        run: echo "Building..."

  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet step
        run: echo "Hello!"
